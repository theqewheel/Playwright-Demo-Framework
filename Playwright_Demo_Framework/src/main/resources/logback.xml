<configuration>

    <!-- ══ Console appender — all logs ══════════════════════════════════════ -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ══ Rolling file — full suite log ════════════════════════════════════ -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/automation.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/automation.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{env}] [%X{methodname}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ══ Sifting appender — one log file per test (uses MDC methodname) ════ -->
    <appender name="SiftingFile" class="ch.qos.logback.classic.sift.SiftingAppender">
        <!-- discriminator reads MDC key "methodname" set in TestListener -->
        <discriminator>
            <key>methodname</key>
            <defaultValue>suite-execution</defaultValue>
        </discriminator>
        <sift>
            <appender name="PerTest-${methodname}"
                      class="ch.qos.logback.core.FileAppender">
                <!-- one file per test in logs/tests/ folder -->
                <file>logs/tests/${methodname}.log</file>
                <append>false</append> <!-- fresh file each run -->
                <encoder>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{env}] [%X{methodname}] %logger{36} - %msg%n</pattern>
                </encoder>
            </appender>
        </sift>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="Console" />
        <appender-ref ref="File" />
        <appender-ref ref="SiftingFile" /> <!-- ✅ per-test log -->
    </root>

</configuration>